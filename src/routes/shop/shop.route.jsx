import { Directory } from "../../components";

import { categories } from "../../constants";

const Shop = () => {
  return (
    <>
      <Directory categories={categories} />;
    </>
  );
};

export default Shop;
